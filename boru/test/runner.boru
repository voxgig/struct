# voxgig/struct boru port — corpus runner entry point.
# Run from the boru/ port directory:  make test
# (The bridge lives in omni.boru; this top level stays minimal — module
# namespace calls are the engine's well-behaved path.)
import "boru:io"
import "./test/omni.boru" end
(OmniRun.main (IO.args)) drop
