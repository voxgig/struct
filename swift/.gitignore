.build/
.swiftpm/
Package.resolved
*.xcodeproj/


# Generated by the Makefile: a symlink to the local voxgig/omni SwiftPM
# package the tests run on. Per-checkout target, so it is never committed.
.omni-runner
