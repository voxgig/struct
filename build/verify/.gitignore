*/node_modules/
*/package-lock.json
go/go.sum
ruby/.gems/
rust/target/
*/.venv/
**/__pycache__/
# Tag-only ports download + build the tagged source under a per-port .build/
*/.build/

# csharp smoke build output (restored/compiled fresh each run)
csharp/bin/
csharp/obj/

# java/kotlin smoke build output (compiled fresh each run)
java/target/
kotlin/target/

# clojure smoke classpath cache
clojure/.cpcache/

# scala smoke build caches
scala/.scala-build/
scala/.bsp/

# dart smoke resolution artifacts
dart/.dart_tool/
dart/pubspec.lock

# rust verify lockfile (regenerated by `cargo update` each run)
rust/Cargo.lock
